害嗨嗨

todo
开始：
点击开始按钮等待三秒再显示结果，等待期间按钮置灰，label文字不断变化
选择随机项组后该组下所有随机项在界面空白处随机位置随机颜色分布

后：

重新定义异常返回，重新定义result，以针对不同异常返回（目前所有错误都无法捕获都返回404）
历史记录查询start大于end时抛异常，统一异常处理，前端页面显示抛出异常报错内容

细节：
点击历史页签自动刷新
点击下一页查询时不带输入框中的查询条件

问题
考虑历史记录过多问题，考虑定时删除，考虑配置定时物理删除
考虑新用户注册没有配置随机项，进入后接口获取空数据前端页面报错问题
LocalDateTime和DateTime类型映射导致带时间条件查询404，后时候String类型传参解决，这是为什么？是否不匹配？
整理注释js方法和后端接口，整理后端接口用法减少代码量
历史记录导出，配置导入
冒烟测试

vue3
修改配置时可以在页面上将随机项拖动到其他组中时间直接移动
学习若依框架的swagger服务
swagger服务返回值结构树明确
报错信息和错误拦截细化定义


1.0 初步实现
1、初步技术选型 spring boot html
2、界面设计，简单前端开发
    三页签：执行、配置、日志
        执行：H1，下拉框选择选项组，执行按钮
        配置（纵向列表）：已有配置列表,新增，删除，修改按钮
        日志（纵向列表）：每次执行多一条，可以导出
3、接口设计，前端页面完善可供基本使用
4、后端开发调试
5、代码优化
6、服务器，docker - k8s部署

2.0 基本实现
1、编写vue3页面，获取服务器部署1.0，
        springcloudalibaba，注册中心，配置中心，
        按照业务模块、各个组件、公共包模块低粒度拆分服务
2、redis缓存执行记录并持久化  主从模式
3、网关服务，应用spring security新增登录注册，过滤拦截
4、添加管理员端
5、部署（测试环境生产环境）

3.0
技术扩展
功能扩展
技术精细化
代码优化
......
